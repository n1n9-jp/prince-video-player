# ローカルの Vite（npm run dev）だけが読む。本番の JS には入れない。
# 本番検索は Worker 秘密変数 YOUTUBE_API_KEY（GitHub Secrets 同名）。
#
# Google Cloud Console で YouTube Data API v3 を有効化し、API キーを発行する。
# ローカル用。ウェブサイト制限するなら http://127.0.0.1:5173/* だけ。
# 本番キーは Worker 秘密変数。ウェブサイト制限は付けない（API 制限のみ）。
VITE_YOUTUBE_API_KEY=
