# Google Cloud Console で YouTube Data API v3 を有効化し、API キーを発行する。
# HTTP リファラ: http://127.0.0.1:5173/* と https://prince-tube.tokyo-air.workers.dev/*
# Cloudflare の本番ビルドでは Workers Builds の環境変数にも同じキーを入れる。
VITE_YOUTUBE_API_KEY=
